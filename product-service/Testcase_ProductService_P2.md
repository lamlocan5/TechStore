# Bảng Kịch Bản Kiểm Thử (Test Cases) - ProductService

Dưới đây là chi tiết các Test Cases được thiết kế cho các phương thức của lớp `ProductService`. Bạn có thể Copy/Paste thẳng vào file Excel báo cáo.

| STT | Mã testcase | Lớp điều khiển | Phương thức | Trường hợp test | Mục tiêu test | Input (Dữ liệu đầu vào) | Expected Output (Kết quả kỳ vọng) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | **PRD_18** | ProductService (Thêm sản phẩm) | `createProduct` | Tạo sản phẩm thành công | Kiểm tra tạo sản phẩm với Category và Brand hợp lệ | Category & Brand tồn tại<br>`ProductRequest request = ProductRequest.builder().name("Valid Product").brandId(brand.getId()).categoryIds(List.of(category.getId())).priceSale(1000L).build();` | Tạo sản phẩm thành công trong DB, trả về `ProductResponse`. |
| 2 | **PRD_19** | ProductService (Thêm sản phẩm) | `createProduct` | Thất bại do Brand không tồn tại | Kiểm tra lỗi khi Brand ID không có trong DB | `brandId` không tồn tại<br>`ProductRequest request = ProductRequest.builder().name("Product No Brand").brandId(999L).priceSale(1000L).build();` | Ném `AppException` với `ErrorCode.BRAND_NOT_FOUND`. |
| 3 | **PRD_20** | ProductService (Thêm sản phẩm) | `createProduct` | Thất bại do Category không tồn tại | Kiểm tra lỗi khi Category ID không có trong DB | `categoryId` không tồn tại<br>`ProductRequest request = ProductRequest.builder().name("Product No Category").brandId(brand.getId()).categoryIds(List.of(999L)).priceSale(1000L).build();` | Ném `AppException` với `ErrorCode.CATEGORY_NOT_FOUND`. |
| 4 | **PRD_21** | ProductService (Thêm sản phẩm) | `createProduct` | Thất bại do tên rỗng | Kiểm tra validate khi tên sản phẩm rỗng | `ProductRequest request = ProductRequest.builder().name("").priceSale(1000L).build();` | Ném Exception do tên rỗng (FAILED nếu hệ thống không validate). |
| 5 | **PRD_22** | ProductService (Thêm sản phẩm) | `createProduct` | Thất bại do tên trùng | Kiểm tra validate ràng buộc Unique tên sản phẩm | Tên "Duplicate Name" đã có trong DB<br>`ProductRequest request = ProductRequest.builder().name("Duplicate Name").priceSale(1000L).build();` | Ném Exception do trùng tên sản phẩm. |
| 6 | **PRD_23** | ProductService (Thêm sản phẩm) | `createProduct` | Thất bại do bỏ trống giá bán | Kiểm tra validate khi không nhập giá bán (null) | `ProductRequest request = ProductRequest.builder().name("No Price Product").priceSale(null).build();` | Ném Exception do giá bán rỗng (FAILED nếu hệ thống không validate). |
| 7 | **PRD_24** | ProductService (Thêm sản phẩm) | `createProduct` | Thất bại do giá bán âm | Kiểm tra validate khi nhập giá bán là số âm | `ProductRequest request = ProductRequest.builder().name("Negative Price Product").priceSale(-500L).build();` | Ném Exception do giá bán âm (FAILED nếu hệ thống không validate). |
| 8 | **PRD_25** | ProductService (Sửa sản phẩm) | `updateProduct` | Cập nhật thông tin sản phẩm thành công | Kiểm tra update sản phẩm với dữ liệu hợp lệ | Product tồn tại<br>`ProductRequest request = ProductRequest.builder().name("New Name").priceSale(2000L).build();` | Cập nhật thông tin thành công trong DB, trả về `ProductResponse`. |
| 9 | **PRD_26** | ProductService (Sửa sản phẩm) | `updateProduct` | Cập nhật thất bại do ID không tồn tại | Kiểm tra lỗi khi cập nhật ID sản phẩm không có | `productId` không tồn tại (999L)<br>`ProductRequest request = ProductRequest.builder().name("New Name").priceSale(1000L).build();` | Ném `AppException` với `ErrorCode.PRODUCT_NOT_FOUND`. |
| 10 | **PRD_27** | ProductService (Sửa sản phẩm) | `updateProduct` | Cập nhật thất bại do Brand ID mới sai | Kiểm tra lỗi khi update sang Brand không tồn tại | `productId` hợp lệ, `brandId` mới không tồn tại<br>`ProductRequest request = ProductRequest.builder().name("New Name").brandId(999L).build();` | Ném `AppException` với `ErrorCode.BRAND_NOT_FOUND`. |
| 11 | **PRD_28** | ProductService (Sửa sản phẩm) | `updateProduct` | Cập nhật thất bại do Category ID mới sai | Kiểm tra lỗi khi update sang Category không tồn tại | `productId` hợp lệ, `categoryId` mới không tồn tại<br>`ProductRequest request = ProductRequest.builder().name("New Name").categoryIds(List.of(999L)).build();` | Ném `AppException` với `ErrorCode.CATEGORY_NOT_FOUND`. |
| 12 | **PRD_29** | ProductService (Sửa sản phẩm) | `updateProduct` | Cập nhật thất bại do tên rỗng | Kiểm tra validate tên để trống khi update | `productId` hợp lệ<br>`ProductRequest request = ProductRequest.builder().name("").priceSale(1000L).build();` | Ném Exception do tên rỗng (FAILED nếu hệ thống không validate). |
| 13 | **PRD_30** | ProductService (Sửa sản phẩm) | `updateProduct` | Cập nhật thất bại do tên trùng | Kiểm tra validate trùng lặp tên với sản phẩm khác | Tên đổi sang trùng với sản phẩm khác<br>`ProductRequest request = ProductRequest.builder().name("Product 1").priceSale(1000L).build();` | Ném Exception do trùng tên sản phẩm. |
| 14 | **PRD_31** | ProductService (Sửa sản phẩm) | `updateProduct` | Cập nhật thất bại do bỏ trống giá bán | Kiểm tra validate khi sửa giá bán thành null | `productId` hợp lệ<br>`ProductRequest request = ProductRequest.builder().name("Product Updated").priceSale(null).build();` | Ném Exception do giá bán rỗng (FAILED nếu hệ thống không validate). |
| 15 | **PRD_32** | ProductService (Sửa sản phẩm) | `updateProduct` | Cập nhật thất bại do giá bán âm | Kiểm tra validate khi sửa giá bán thành số âm | `productId` hợp lệ<br>`ProductRequest request = ProductRequest.builder().name("Product Updated").priceSale(-1000L).build();` | Ném Exception do giá bán âm (FAILED nếu hệ thống không validate). |
| 16 | **PRD_33** | ProductService (Xoá sản phẩm) | `deleteProduct` | Xoá sản phẩm thành công | Kiểm tra thao tác xóa theo ID hợp lệ | `productId` tồn tại | Xóa sản phẩm thành công khỏi DB. |
| 17 | **PRD_34** | ProductService (Xoá sản phẩm) | `deleteProduct` | Xoá thất bại do ID không tồn tại | Kiểm tra lỗi khi xóa ID sản phẩm không có | `productId` không tồn tại (999L) | Ném `AppException` với `ErrorCode.PRODUCT_NOT_FOUND`. |
| 18 | **PRD_35** | ProductService (Xem sản phẩm) | `getProductById` | Xem chi tiết sản phẩm thành công | Kiểm tra lấy thông tin sản phẩm theo ID | `productId` tồn tại | Lấy thông tin thành công, trả về `ProductResponse`. |
| 19 | **PRD_36** | ProductService (Xem sản phẩm) | `getProductById` | Xem chi tiết thất bại do ID không tồn tại | Kiểm tra lỗi khi lấy ID sản phẩm không có | `productId` không tồn tại (999L) | Ném `AppException` với `ErrorCode.PRODUCT_NOT_FOUND`. |
| 20 | **PRD_37** | ProductService (Xem sản phẩm) | `getAllProducts` | Lấy toàn bộ danh sách phân trang | Kiểm tra kết quả phân trang chính xác | DB có sẵn sản phẩm<br>`PageRequest pageRequest = PageRequest.of(0, 2);` | Lấy danh sách sản phẩm thành công, trả về `Page<ProductResponse>`. |
| 21 | **PRD_38** | ProductService (Tìm kiếm sản phẩm) | `searchByName` | Tìm kiếm danh sách có kết quả | Kiểm tra tìm kiếm chuỗi (LIKE/Fuzzy) theo tên | DB có "Macbook Pro"<br>`String keyword = "Macbook";` | Trả về danh sách các `ProductResponse` chứa từ khóa. |
| 22 | **PRD_39** | ProductService (Tìm kiếm sản phẩm) | `searchByName` | Không tìm thấy kết quả | Kiểm tra trả về rỗng khi từ khoá sai/không có thật | `String wrongKeyword = "ThisProductDoesNotExist12345";` | Trả về danh sách rỗng. |
| 23 | **PRD_40** | ProductService (Tìm kiếm sản phẩm) | `searchProductsWithCategory` | Tìm kiếm kết hợp thêm filter categoryId | Kiểm tra tìm kiếm kết hợp nhiều filter | Sản phẩm "Macbook Air" thuộc `categoryId`, `brandId`<br>`PageRequest pageRequest = PageRequest.of(0, 10);` | Trả về danh sách sản phẩm khớp với cả từ khoá và categoryId. |
| 24 | **PRD_41** | ProductVariantService (Thêm biến thể) | `create` | Tạo biến thể thành công | Kiểm tra tạo biến thể với `productId` hợp lệ | `productId` tồn tại<br>`ProductVariantRequest request = ProductVariantRequest.builder().productId(id).sku("SKU001").color("Đen").priceSale(1500L).stock(10).build();` | Tạo biến thể thành công trong DB, trả về `ProductVariantResponse`. |
| 25 | **PRD_42** | ProductVariantService (Thêm biến thể) | `create` | Thất bại do productId không tồn tại | Kiểm tra lỗi khi `productId` không có trong DB | `productId` không tồn tại (999L) | Ném `AppException` với `ErrorCode.PRODUCT_NOT_FOUND`. |
| 26 | **PRD_43** | ProductVariantService (Thêm biến thể) | `create` | Thất bại do SKU rỗng | Kiểm tra validate khi SKU để trống | `ProductVariantRequest request = ProductVariantRequest.builder().sku("").priceSale(1500L).stock(10).build();` | Ném Exception do SKU rỗng (FAILED nếu không validate). |
| 27 | **PRD_44** | ProductVariantService (Thêm biến thể) | `create` | Thất bại do trùng SKU | Kiểm tra validate ràng buộc Unique của SKU | SKU đã tồn tại trong DB<br>`ProductVariantRequest request = ProductVariantRequest.builder().sku("SKU_EXISTED").build();` | Ném Exception do trùng SKU (FAILED nếu không có validate rõ ràng). |
| 28 | **PRD_45** | ProductVariantService (Thêm biến thể) | `create` | Thất bại do giá bán âm | Kiểm tra validate khi giá bán là số âm | `ProductVariantRequest request = ProductVariantRequest.builder().sku("SKU003").priceSale(-500L).stock(10).build();` | Ném Exception do giá bán âm (FAILED nếu không validate). |
| 29 | **PRD_46** | ProductVariantService (Thêm biến thể) | `create` | Thất bại do số lượng tồn kho âm | Kiểm tra validate khi số lượng kho là số âm | `ProductVariantRequest request = ProductVariantRequest.builder().sku("SKU004").priceSale(1500L).stock(-5).build();` | Ném Exception do tồn kho âm (FAILED nếu không validate). |
| 30 | **PRD_47** | ProductVariantService (Xoá biến thể) | `delete` | Xoá biến thể thành công | Kiểm tra thao tác xóa biến thể theo ID hợp lệ | `variantId` tồn tại | Xóa biến thể thành công khỏi DB. |
| 31 | **PRD_48** | ProductVariantService (Xoá biến thể) | `delete` | Xoá thất bại do ID không tồn tại | Kiểm tra ném lỗi khi ID cần xóa không tồn tại | `variantId` không tồn tại (999L) | Ném Exception thông báo không tìm thấy ID (FAILED nếu không validate). |
| 32 | **PRD_49** | ProductVariantService (Xem biến thể) | `getById` | Xem chi tiết thành công | Kiểm tra lấy chi tiết biến thể theo ID | `variantId` tồn tại | Trả về `ProductVariantResponse` tương ứng. |
| 33 | **PRD_50** | ProductVariantService (Xem biến thể) | `getById` | Xem chi tiết thất bại do ID sai | Kiểm tra lỗi khi xem ID không tồn tại | `variantId` không tồn tại (999L) | Ném `AppException` với `ErrorCode.PRODUCT_VARIANT_NOT_FOUND`. |
| 34 | **PRD_51** | ProductVariantService (Xem biến thể) | `findByProduct` | Xem danh sách theo sản phẩm | Lấy toàn bộ biến thể của 1 sản phẩm | `productId` tồn tại | Trả về danh sách `ProductVariantResponse`. |
| 35 | **PRD_52** | ProductVariantService (Quản lý tồn kho) | `addStock` | Cộng tồn kho thành công | Kiểm tra tăng số lượng tồn kho | `variantId` tồn tại, `quantity` = 5 | Cập nhật số lượng kho trong DB (+5). |
| 36 | **PRD_53** | ProductVariantService (Quản lý tồn kho) | `addStock` | Cộng tồn kho thất bại | Kiểm tra lỗi khi số lượng cộng <= 0 | `quantity` = 0 hoặc `quantity` = -5 | Ném `AppException` với `ErrorCode.INVALID_STOCK_QUANTITY`. |
| 37 | **PRD_54** | ProductVariantService (Quản lý tồn kho) | `reduceStock` | Trừ tồn kho thành công | Kiểm tra giảm số lượng tồn kho | `variantId` tồn tại, `quantity` = 5 (kho đang có 20) | Cập nhật số lượng kho trong DB (-5). |
| 38 | **PRD_55** | ProductVariantService (Quản lý tồn kho) | `reduceStock` | Trừ tồn kho thất bại số lượng sai | Kiểm tra lỗi khi số lượng trừ <= 0 | `quantity` = 0 | Ném `AppException` với `ErrorCode.INVALID_STOCK_QUANTITY`. |
| 39 | **PRD_56** | ProductVariantService (Quản lý tồn kho) | `reduceStock` | Trừ tồn kho thất bại do không đủ hàng | Kiểm tra lỗi khi trừ nhiều hơn kho đang có | Kho có 5, `quantity` = 10 | Ném `AppException` với `ErrorCode.INSUFFICIENT_STOCK`. |
| 40 | **PRD_57** | ProductVariantService (Thống kê) | `getStatistics` | Lấy thống kê tồn kho thành công | Kiểm tra hàm tính tổng số lượng tồn kho và tổng loại | Có 2 biến thể trong DB | Trả về `VariantStatsResponse` chứa tổng kho và tổng loại. |
| 41 | **PRD_58** | ReviewService (Thêm Đánh giá) | `createReview` | Thêm review thành công | Kiểm tra tạo review hợp lệ (đã mua, có rating và comment) | `userId` đã mua `productId`<br>`request` có `rating=5`, `comment="Sản phẩm rất tốt"` | Tạo review thành công trong DB, trả về `ReviewResponse`. |
| 42 | **PRD_59** | ReviewService (Thêm Đánh giá) | `createReview` | Thất bại do sản phẩm không tồn tại | Kiểm tra lỗi khi truyền `productId` không có trong hệ thống | `productId` không tồn tại (999L) | Ném `AppException` với `ErrorCode.PRODUCT_NOT_FOUND`. |
| 43 | **PRD_60** | ReviewService (Thêm Đánh giá) | `createReview` | Thất bại do đánh giá sao khi chưa mua | Kiểm tra hệ thống chặn gửi `rating` khi chưa mua hàng | `userId` chưa mua `productId`<br>`request` cố gửi `rating=5` | Ném `AppException` với `ErrorCode.REVIEW_RATING_NOT_ALLOWED`. |
| 44 | **PRD_61** | ReviewService (Thêm Đánh giá) | `createReview` | Thất bại do để trống nội dung | Kiểm tra bắt lỗi khi gửi review rỗng (không `rating`, không `comment`) | `rating=null`, `comment=""` | Ném `AppException` với `ErrorCode.REVIEW_REQUIRED_FIELDS`. |
| 45 | **PRD_62** | ReviewService (Thêm Đánh giá) | `createReview` | Thất bại do số sao không hợp lệ | Kiểm tra hệ thống chặn số sao nằm ngoài 1-5 | `rating=6` hoặc `rating=0` | Ném `AppException` với `ErrorCode.INVALID_RATING`. |
| 46 | **PRD_63** | ReviewService (Thêm Đánh giá) | `createReview` | Thất bại do đánh giá 2 lần | Kiểm tra hệ thống chặn 1 người dùng đánh giá nhiều lần trên 1 sản phẩm | Đã có review trước đó với `rating=4`, gửi thêm request mới có `rating=5` | Ném `AppException` với `ErrorCode.REVIEW_ALREADY_EXISTS`. |
| 47 | **PRD_64** | ReviewService (Thêm Đánh giá) | `createReview` | Thất bại do chưa mua hàng đã gửi comment | Kiểm tra bắt lỗi khi người dùng chưa mua hàng mà gửi bình luận | `userId` chưa mua `productId`<br>`request` có `comment` nhưng không có `rating` | Ném Exception chặn bình luận (FAILED nếu code hiện tại không chặn). |
| 48 | **PRD_65** | ReviewService (Xem Đánh giá) | `getReviewsByProductId` | Lấy danh sách review thành công | Kiểm tra lấy danh sách phân trang cho 1 sản phẩm | `productId` tồn tại<br>`PageRequest` hợp lệ | Trả về đối tượng `Page<ReviewResponse>` chứa các review. |
| 49 | **PRD_66** | ReviewService (Xem Đánh giá) | `getReviewsByProductId` | Lấy danh sách thất bại do sản phẩm không tồn tại | Kiểm tra lỗi khi gọi API lấy danh sách cho 1 ID sản phẩm sai | `productId` không tồn tại (999L) | Ném `AppException` với `ErrorCode.PRODUCT_NOT_FOUND`. |
