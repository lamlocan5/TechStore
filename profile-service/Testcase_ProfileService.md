# Bảng Kịch Bản Kiểm Thử (Test Cases) - ProfileService

Dưới đây là chi tiết các Test Cases được thiết kế cho các phương thức của lớp `UserProfileService` và `AddressService`. Bạn có thể Copy/Paste thẳng vào file Excel báo cáo.

| STT | Mã testcase | Lớp điều khiển | Phương thức | Trường hợp test | Mục tiêu test | Input (Dữ liệu đầu vào) | Expected Output (Kết quả kỳ vọng) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | **PRF_01** | UserProfileService (Thêm profile) | `createProfile` | Tạo profile thành công | Kiểm tra tạo profile với đầy đủ thông tin hợp lệ | `userId`, `firstName`, `lastName`, `email`, `phone`, `dob`, `avatar` hợp lệ<br>`ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-001").firstName("Nguyen").lastName("Van A").email("nguyenvana@example.com").phone("0901234561").dob(LocalDate.of(1995,5,20)).build();` | Tạo profile thành công trong DB, trả về `UserProfileResponse`. |
| 2 | **PRF_02** | UserProfileService (Thêm profile) | `createProfile` | Thất bại do email đã tồn tại | Kiểm tra lỗi khi email đã được dùng bởi profile khác | Email `"duplicate@example.com"` đã có trong DB<br>`ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-002").email("duplicate@example.com").phone("0900000002").build();` | Ném `AppException` với `ErrorCode.EMAIL_EXISTED`. |
| 3 | **PRF_03** | UserProfileService (Thêm profile) | `createProfile` | Thất bại do số điện thoại đã tồn tại | Kiểm tra lỗi khi SĐT đã được dùng bởi profile khác | SĐT `"0911111111"` đã có trong DB<br>`ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-003").email("levanc@example.com").phone("0911111111").build();` | Ném `AppException` với `ErrorCode.PHONE_EXISTED`. |
| 4 | **PRF_04** | UserProfileService (Thêm profile) | `createProfile` | Thất bại khi email và phone đều để rỗng | Kiểm tra validate khi cả email và SĐT đều là chuỗi rỗng | `ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-004").email("").phone("").build();` | Ném Exception do email và phone rỗng (FAILED nếu hệ thống không validate). |
| 5 | **PRF_05** | UserProfileService (Thêm profile) | `createProfile` | Thất bại do userId null | Kiểm tra validate khi userId null, vi phạm NOT NULL constraint | `ProfileCreationRequest request = ProfileCreationRequest.builder().userId(null).email("testuser@example.com").phone("0922222222").build();` | Ném Exception do userId null vi phạm DB constraint (FAILED nếu hệ thống không validate). |
| 6 | **PRF_06** | UserProfileService (Thêm profile) | `createProfile` | Thất bại do email không có đuôi @gmail.com | Kiểm tra validate định dạng email | `ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-006").email("nguyenvanf_invalid").phone("0901234506").build();` | Ném Exception do email sai định dạng (FAILED nếu hệ thống không validate). |
| 7 | **PRF_07** | UserProfileService (Thêm profile) | `createProfile` | Thất bại do số điện thoại không đủ 10 số | Kiểm tra validate độ dài SĐT | `ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-007").email("nguyenvang@gmail.com").phone("090123").build();` | Ném Exception do SĐT không đủ 10 chữ số (FAILED nếu hệ thống không validate). |
| 8 | **PRF_08** | UserProfileService (Thêm profile) | `createProfile` | Thất bại do số điện thoại không bắt đầu bằng số 0 | Kiểm tra validate tiền tố SĐT Việt Nam | `ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-008").email("nguyenvanh@gmail.com").phone("1901234508").build();` | Ném Exception do SĐT không bắt đầu bằng số 0 (FAILED nếu hệ thống không validate). |
| 9 | **PRF_09** | UserProfileService (Thêm profile) | `createProfile` | Thất bại do ngày sinh lớn hơn ngày hiện tại | Kiểm tra validate ngày sinh không hợp lệ trong tương lai | `ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-009").email("nguyenvani@gmail.com").phone("0901234509").dob(LocalDate.now().plusDays(1)).build();` | Ném Exception do ngày sinh trong tương lai (FAILED nếu hệ thống không validate). |
| 10 | **PRF_10** | UserProfileService (Sửa profile) | `updateProfile` | Cập nhật profile thành công | Kiểm tra update profile với dữ liệu hợp lệ | Profile `"user-006"` tồn tại<br>`ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-006").firstName("Tran").lastName("Thi B Updated").email("user006updated@example.com").phone("0930000066").dob(LocalDate.of(1992,3,15)).build();` | Cập nhật thông tin thành công trong DB, trả về `UserProfileResponse`. |
| 11 | **PRF_11** | UserProfileService (Sửa profile) | `updateProfile` | Cập nhật thất bại do email mới đã được dùng bởi user khác | Kiểm tra lỗi khi đổi sang email đã tồn tại của user khác | 2 profile `"user-007a"` và `"user-007b"` tồn tại<br>`ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-007b").email("user007a@example.com").build();` | Ném `AppException` với `ErrorCode.EMAIL_EXISTED`. |
| 12 | **PRF_12** | UserProfileService (Sửa profile) | `updateProfile` | Cập nhật thất bại do số điện thoại mới đã được dùng bởi user khác | Kiểm tra lỗi khi đổi sang SĐT đã tồn tại của user khác | 2 profile `"user-008a"` và `"user-008b"` tồn tại<br>`ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-008b").phone("0930000081").build();` | Ném `AppException` với `ErrorCode.PHONE_EXISTED`. |
| 13 | **PRF_13** | UserProfileService (Sửa profile) | `updateProfile` | Cập nhật thất bại do email không có đuôi @gmail.com | Kiểm tra validate định dạng email khi cập nhật | Profile `"user-009"` tồn tại<br>`ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-009").email("user009_invalid_email").phone("0930000090").build();` | Ném Exception do email sai định dạng (FAILED nếu hệ thống không validate). |
| 14 | **PRF_14** | UserProfileService (Sửa profile) | `updateProfile` | Cập nhật thất bại do số điện thoại không đủ 10 số | Kiểm tra validate độ dài SĐT khi cập nhật | Profile `"user-010"` tồn tại<br>`ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-010").phone("09300").build();` | Ném Exception do SĐT không đủ 10 chữ số (FAILED nếu hệ thống không validate). |
| 15 | **PRF_15** | UserProfileService (Sửa profile) | `updateProfile` | Cập nhật thất bại do số điện thoại không bắt đầu bằng số 0 | Kiểm tra validate tiền tố SĐT khi cập nhật | Profile `"user-011"` tồn tại<br>`ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-011").phone("1930000110").build();` | Ném Exception do SĐT không bắt đầu bằng số 0 (FAILED nếu hệ thống không validate). |
| 16 | **PRF_16** | UserProfileService (Sửa profile) | `updateProfile` | Cập nhật thất bại do ngày sinh lớn hơn ngày hiện tại | Kiểm tra validate ngày sinh không hợp lệ khi cập nhật | Profile `"user-012"` tồn tại<br>`ProfileCreationRequest request = ProfileCreationRequest.builder().userId("user-012").dob(LocalDate.now().plusDays(1)).build();` | Ném Exception do ngày sinh trong tương lai (FAILED nếu hệ thống không validate). |
| 17 | **PRF_17** | UserProfileService (Xem profile) | `getProfile` | Xem chi tiết profile thành công theo ID hồ sơ | Kiểm tra lấy thông tin profile theo profile ID | `profileId` tồn tại trong DB | Lấy thông tin thành công, trả về `UserProfileResponse`. |
| 18 | **PRF_18** | UserProfileService (Xem profile) | `getProfile` | Xem profile thất bại do ID hồ sơ không tồn tại | Kiểm tra lỗi khi lấy profile ID không có trong DB | `profileId = "non-existent-id-99999"` | Ném `AppException` với `ErrorCode.PROFILE_NOT_FOUND`. |
| 19 | **PRF_19** | UserProfileService (Xem profile) | `getProfileByUserId` | Lấy profile thành công theo userId | Kiểm tra lấy thông tin profile theo userId | `userId = "user-013"` tồn tại trong DB | Lấy thông tin thành công, trả về `UserProfileResponse`. |
| 20 | **PRF_20** | UserProfileService (Xem profile) | `getProfileByUserId` | Lấy profile theo userId không tồn tại trả về null | Kiểm tra trường hợp userId không có trong DB không ném exception | `userId = "user-not-exist-99999"` không có trong DB | Trả về `null` (không ném exception). |
| 21 | **PRF_21** | UserProfileService (Xem profile) | `getAllProfiles` | Lấy toàn bộ danh sách profile thành công (ADMIN) | Kiểm tra quyền ADMIN và kết quả trả về danh sách | Có sẵn 3 profile trong DB<br>Gọi với quyền `ADMIN` | Trả về `List<UserProfileResponse>` chứa tất cả profile. |
| 22 | **PRF_22** | UserProfileService (Xem profile) | `getAllProfiles(Pageable)` | Lấy danh sách profile phân trang thành công (ADMIN) | Kiểm tra kết quả phân trang chính xác với quyền ADMIN | Có sẵn 5 profile trong DB<br>`PageRequest pageRequest = PageRequest.of(0, 2);`<br>Gọi với quyền `ADMIN` | Trả về `Page<UserProfileResponse>` với `size=2`, `totalElements>=5`. |
| 23 | **PRF_23** | AddressService (Thêm địa chỉ) | `createAddress` | Tạo địa chỉ đầu tiên thành công và tự động là mặc định | Kiểm tra logic tự động đặt default cho địa chỉ đầu tiên của user | User chưa có địa chỉ nào<br>`AddressRequest request = AddressRequest.builder().userId("user-c01").receiverName("Nguyen Van A").phone("0901234501").addressLine("123 Nguyen Trai").isDefault(false).build();` | Tạo địa chỉ thành công, `isDefault = true` tự động. |
| 24 | **PRF_24** | AddressService (Thêm địa chỉ) | `createAddress` | Tạo địa chỉ thứ 2 không đánh dấu default, isDefault = false | Kiểm tra địa chỉ thứ 2 không ghi đè default khi không yêu cầu | User đã có 1 địa chỉ default<br>`AddressRequest request = AddressRequest.builder().userId("user-c02").addressLine("456 Le Loi").isDefault(false).build();` | Tạo thành công, địa chỉ mới có `isDefault = false`, địa chỉ cũ vẫn là default. |
| 25 | **PRF_25** | AddressService (Thêm địa chỉ) | `createAddress` | Tạo địa chỉ thứ 2 với isDefault=true, địa chỉ cũ mất default | Kiểm tra logic chuyển default khi thêm địa chỉ mới với isDefault=true | User đã có 1 địa chỉ default<br>`AddressRequest request = AddressRequest.builder().userId("user-c03").addressLine("789 Tran Hung Dao").isDefault(true).build();` | Tạo thành công, địa chỉ mới `isDefault = true`, địa chỉ cũ `isDefault = false`. |
| 26 | **PRF_26** | AddressService (Thêm địa chỉ) | `createAddress` | Thất bại do userId null | Kiểm tra validate khi userId null, vi phạm NOT NULL constraint | `AddressRequest request = AddressRequest.builder().userId(null).addressLine("999 Test Street").build();` | Ném Exception do userId null vi phạm DB constraint (FAILED nếu hệ thống không validate). |
| 27 | **PRF_27** | AddressService (Thêm địa chỉ) | `createAddress` | Thất bại do addressLine để rỗng | Kiểm tra validate khi địa chỉ cụ thể để trống | `AddressRequest request = AddressRequest.builder().userId("user-c05").addressLine("").build();` | Ném Exception do addressLine rỗng (FAILED nếu hệ thống không validate). |
| 28 | **PRF_28** | AddressService (Sửa địa chỉ) | `updateAddress` | Cập nhật thông tin địa chỉ thành công | Kiểm tra update địa chỉ với dữ liệu hợp lệ | Địa chỉ `addressId` tồn tại và thuộc về `userId`<br>`AddressRequest request = AddressRequest.builder().userId("user-u01").receiverName("Tran Thi B Updated").phone("0912345678").addressLine("456 Le Loi Moi").isDefault(true).build();` | Cập nhật thành công trong DB, trả về `AddressResponse`. |
| 29 | **PRF_29** | AddressService (Sửa địa chỉ) | `updateAddress` | Cập nhật thất bại do ID địa chỉ không tồn tại | Kiểm tra lỗi khi update ID không có trong DB | `addressId = 999999` không tồn tại | Ném `AppException` với `ErrorCode.ADDRESS_NOT_FOUND`. |
| 30 | **PRF_30** | AddressService (Sửa địa chỉ) | `updateAddress` | Cập nhật thất bại do địa chỉ thuộc về user khác | Kiểm tra phân quyền sở hữu địa chỉ khi update | Địa chỉ thuộc `"user-u03a"`, request gửi với `userId = "user-u03b"` | Ném `AppException` với `ErrorCode.UNAUTHORIZED`. |
| 31 | **PRF_31** | AddressService (Sửa địa chỉ) | `updateAddress` | Đặt địa chỉ thành default, các địa chỉ khác mất default | Kiểm tra logic chuyển default khi update địa chỉ sang isDefault=true | User có 2 địa chỉ, địa chỉ 1 là default<br>Update địa chỉ 2 với `isDefault = true` | Địa chỉ 2 `isDefault = true`, địa chỉ 1 `isDefault = false`. |
| 32 | **PRF_32** | AddressService (Sửa địa chỉ) | `updateAddress` | Bỏ default khi chỉ có 1 địa chỉ duy nhất, địa chỉ vẫn giữ nguyên là default | Kiểm tra hệ thống giữ nguyên default khi bỏ default cho địa chỉ duy nhất | User chỉ có 1 địa chỉ đang là default<br>Update với `isDefault = false` | Địa chỉ vẫn giữ `isDefault = true` (hệ thống không cho bỏ default địa chỉ duy nhất). |
| 33 | **PRF_33** | AddressService (Sửa địa chỉ) | `updateAddress` | Bỏ default khi có nhiều địa chỉ, không có địa chỉ nào được tự động chuyển thành default | Kiểm tra hệ thống không tự động chuyển default sang địa chỉ khác | User có 2 địa chỉ, địa chỉ 1 là default<br>Bỏ default địa chỉ 1 với `isDefault = false` | Không có địa chỉ nào là default sau khi bỏ (FAILED nếu hệ thống tự động chuyển default). |
| 34 | **PRF_34** | AddressService (Xem địa chỉ) | `getAddressById` | Xem chi tiết địa chỉ thành công theo ID và userId hợp lệ | Kiểm tra lấy thông tin địa chỉ khi đúng chủ sở hữu | `addressId` tồn tại, `userId = "user-g01"` là chủ sở hữu | Lấy thông tin thành công, trả về `AddressResponse`. |
| 35 | **PRF_35** | AddressService (Xem địa chỉ) | `getAddressById` | Xem địa chỉ thất bại do ID không tồn tại | Kiểm tra lỗi khi lấy địa chỉ ID không có trong DB | `addressId = 999999` không tồn tại | Ném `AppException` với `ErrorCode.ADDRESS_NOT_FOUND`. |
| 36 | **PRF_36** | AddressService (Xem địa chỉ) | `getAddressById` | Xem địa chỉ thất bại do địa chỉ thuộc về user khác | Kiểm tra phân quyền sở hữu địa chỉ khi xem | Địa chỉ thuộc `"user-g03a"`, gọi với `userId = "user-g03b"` | Ném `AppException` với `ErrorCode.UNAUTHORIZED`. |
| 37 | **PRF_37** | AddressService (Xem địa chỉ) | `getAddressByUserId` | Lấy địa chỉ theo userId thành công khi có địa chỉ default | Kiểm tra hệ thống trả về đúng địa chỉ mặc định | User có 2 địa chỉ, địa chỉ 2 là default | Trả về `AddressResponse` của địa chỉ có `isDefault = true`. |
| 38 | **PRF_38** | AddressService (Xem địa chỉ) | `getAddressByUserId` | Lấy địa chỉ khi không có địa chỉ default phải trả về rỗng | Kiểm tra hệ thống không trả về địa chỉ đầu tiên khi không có default | User có 1 địa chỉ nhưng `isDefault = false` | Trả về `null/rỗng` (FAILED nếu hệ thống trả về địa chỉ đầu tiên thay vì rỗng). |
| 39 | **PRF_39** | AddressService (Xem địa chỉ) | `getAllAddressesByUserId` | Lấy toàn bộ danh sách địa chỉ của user thành công | Kiểm tra kết quả trả về danh sách đầy đủ | User có 3 địa chỉ trong DB | Trả về `List<AddressResponse>` chứa đúng 3 địa chỉ. |
| 40 | **PRF_40** | AddressService (Xem địa chỉ) | `getAllAddressesByUserId` | Lấy toàn bộ địa chỉ khi user không có địa chỉ nào trả về danh sách rỗng | Kiểm tra trả về list rỗng khi user chưa có địa chỉ | User không có địa chỉ nào trong DB | Trả về `List<AddressResponse>` rỗng (không ném exception). |
| 41 | **PRF_41** | AddressService (Xóa địa chỉ) | `deleteAddress` | Xóa địa chỉ thành công theo ID hợp lệ | Kiểm tra thao tác xóa địa chỉ theo ID và userId hợp lệ | `addressId` tồn tại, `userId = "user-d01"` là chủ sở hữu | Xóa địa chỉ thành công khỏi DB, `existsById` trả về `false`. |
| 42 | **PRF_42** | AddressService (Xóa địa chỉ) | `deleteAddress` | Xóa thất bại do ID địa chỉ không tồn tại | Kiểm tra lỗi khi xóa địa chỉ ID không có trong DB | `addressId = 999999` không tồn tại | Ném `AppException` với `ErrorCode.ADDRESS_NOT_FOUND`. |
| 43 | **PRF_43** | AddressService (Xóa địa chỉ) | `deleteAddress` | Xóa thất bại do địa chỉ thuộc về user khác | Kiểm tra phân quyền sở hữu địa chỉ khi xóa | Địa chỉ thuộc `"user-d03a"`, gọi xóa với `userId = "user-d03b"` | Ném `AppException` với `ErrorCode.UNAUTHORIZED`, địa chỉ vẫn còn trong DB. |
