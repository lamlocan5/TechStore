
# Bảng Kịch Bản Kiểm Thử (Test Cases) - OrderService

Dưới đây là chi tiết các Test Cases được thiết kế cho các phương thức của `VoucherService`, `UserVoucherService`, `CartService` và `OrderService`. Bạn có thể Copy/Paste thẳng vào file Excel báo cáo.

| STT | Mã testcase | Lớp điều khiển | Phương thức | Trường hợp test | Mục tiêu test | Input (Dữ liệu đầu vào) | Expected Output (Kết quả kỳ vọng) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | **ORD_01** | VoucherService (Thêm voucher) | `createVoucher` | Tạo voucher giảm phần trăm thành công | Kiểm tra tạo voucher PERCENT với đầy đủ thông tin hợp lệ | `VoucherRequest request = VoucherRequest.builder().code("SUMMER10").name("Summer Sale").discountType(DiscountType.PERCENT).discountValue(10L).minOrderTotal(100000L).startAt(now-1d).endAt(now+30d).maxUsage(100).maxPerUser(1).status(1).minRankRequired(MembershipRank.BRONZE).build();` | Tạo thành công trong DB, trả về `VoucherResponse` với đầy đủ thông tin. |
| 2 | **ORD_02** | VoucherService (Thêm voucher) | `createVoucher` | Tạo voucher giảm tiền cố định thành công | Kiểm tra tạo voucher AMOUNT hợp lệ | `VoucherRequest request = VoucherRequest.builder().code("FLAT50K").name("Flat 50K").discountType(DiscountType.AMOUNT).discountValue(50000L).minOrderTotal(200000L).startAt(now-1d).endAt(now+30d).status(1).minRankRequired(MembershipRank.SILVER).build();` | Tạo thành công trong DB, trả về `VoucherResponse`. |
| 3 | **ORD_03** | VoucherService (Thêm voucher) | `createVoucher` | Thất bại khi không điền minRankRequired | Kiểm tra validate bắt buộc minRankRequired | `VoucherRequest request = VoucherRequest.builder().code("NORANK01").discountType(DiscountType.AMOUNT).discountValue(10000L).status(1).minRankRequired(null).build();` | Ném Exception (FAILED nếu hệ thống tự động gán BRONZE thay vì báo lỗi). |
| 4 | **ORD_04** | VoucherService (Thêm voucher) | `createVoucher` | Thất bại do code voucher đã tồn tại | Kiểm tra ràng buộc UNIQUE trên cột `code` | Code `"DUPCODE"` đã tồn tại trong DB<br>`VoucherRequest request = VoucherRequest.builder().code("DUPCODE").discountType(DiscountType.AMOUNT).discountValue(10000L).status(1).minRankRequired(MembershipRank.BRONZE).build();` | Ném Exception do vi phạm UNIQUE constraint. |
| 5 | **ORD_05** | VoucherService (Thêm voucher) | `createVoucher` | Thất bại do code null | Kiểm tra ràng buộc NOT NULL trên cột `code` | `VoucherRequest request = VoucherRequest.builder().code(null).discountType(DiscountType.AMOUNT).discountValue(10000L).status(1).minRankRequired(MembershipRank.BRONZE).build();` | Ném Exception do vi phạm NOT NULL constraint. |
| 6 | **ORD_06** | VoucherService (Thêm voucher) | `createVoucher` | Thất bại do ngày bắt đầu sau ngày kết thúc | Kiểm tra validate khoảng thời gian hợp lệ | `VoucherRequest request = VoucherRequest.builder().code("DATETEST").startAt(now+10d).endAt(now+1d).discountValue(10000L).status(1).minRankRequired(MembershipRank.BRONZE).build();` | Ném Exception do startAt > endAt (FAILED nếu hệ thống không validate). |
| 7 | **ORD_07** | VoucherService (Thêm voucher) | `createVoucher` | Thất bại do discountValue <= 0 | Kiểm tra validate giá trị giảm giá không hợp lệ | `VoucherRequest request = VoucherRequest.builder().code("NEGVAL01").discountValue(-5000L).status(1).minRankRequired(MembershipRank.BRONZE).build();` | Ném Exception do discountValue <= 0 (FAILED nếu hệ thống không validate). |
| 8 | **ORD_08** | VoucherService (Sửa voucher) | `updateVoucher` | Cập nhật voucher thành công | Kiểm tra update voucher với dữ liệu hợp lệ | Voucher `id` hợp lệ tồn tại trong DB<br>`VoucherRequest request = VoucherRequest.builder().code("UPDATED01").name("Updated Name").discountValue(20000L).status(1).minRankRequired(MembershipRank.GOLD).build();` | Cập nhật thành công trong DB, trả về `VoucherResponse` với thông tin mới. |
| 9 | **ORD_09** | VoucherService (Sửa voucher) | `updateVoucher` | Thất bại do ID voucher không tồn tại | Kiểm tra lỗi khi update voucher ID không có trong DB | `id = 999999` không tồn tại trong DB | Ném `AppException` với `ErrorCode.VOUCHER_NOT_FOUND`. |
| 10 | **ORD_10** | VoucherService (Sửa voucher) | `updateVoucher` | Đổi loại giảm giá từ AMOUNT sang PERCENT thành công | Kiểm tra thay đổi `discountType` khi update | Voucher cũ `discountType=AMOUNT`<br>`VoucherRequest request = VoucherRequest.builder().discountType(DiscountType.PERCENT).discountValue(15L).minRankRequired(MembershipRank.BRONZE).build();` | Cập nhật thành công, `discountType=PERCENT`, `discountValue=15`. |
| 11 | **ORD_11** | VoucherService (Sửa voucher) | `updateVoucher` | Thất bại do code mới trùng với voucher khác | Kiểm tra UNIQUE constraint khi đổi code | Code mới `"DUPCODE"` đã tồn tại ở voucher khác<br>Cập nhật voucher hiện tại sang code `"DUPCODE"` | Ném Exception do vi phạm UNIQUE constraint. |
| 12 | **ORD_12** | VoucherService (Sửa voucher) | `updateVoucher` | Thất bại khi không điền minRankRequired khi sửa | Kiểm tra validate bắt buộc minRankRequired khi update | Voucher tồn tại<br>`VoucherRequest request = VoucherRequest.builder().code("UPD012").discountValue(10000L).minRankRequired(null).build();` | Ném Exception (FAILED nếu hệ thống tự động gán BRONZE thay vì báo lỗi). |
| 13 | **ORD_13** | VoucherService (Sửa voucher) | `updateVoucher` | Thất bại do ngày bắt đầu sau ngày kết thúc khi sửa | Kiểm tra validate khoảng thời gian khi update | Voucher tồn tại<br>`VoucherRequest request = VoucherRequest.builder().startAt(now+10d).endAt(now+1d).minRankRequired(MembershipRank.BRONZE).build();` | Ném Exception do startAt > endAt (FAILED nếu hệ thống không validate). |
| 14 | **ORD_14** | VoucherService (Sửa voucher) | `updateVoucher` | Thất bại do discountValue <= 0 khi sửa | Kiểm tra validate giá trị giảm giá khi update | Voucher tồn tại<br>`VoucherRequest request = VoucherRequest.builder().discountValue(0L).minRankRequired(MembershipRank.BRONZE).build();` | Ném Exception do discountValue <= 0 (FAILED nếu hệ thống không validate). |
| 15 | **ORD_15** | VoucherService (Xem voucher) | `getVoucherById` | Lấy chi tiết voucher thành công theo ID hợp lệ | Kiểm tra lấy thông tin voucher theo ID | Voucher `id` tồn tại trong DB | Trả về `VoucherResponse` với đầy đủ thông tin đúng. |
| 16 | **ORD_16** | VoucherService (Xem voucher) | `getVoucherById` | Lấy voucher thất bại do ID không tồn tại | Kiểm tra lỗi khi ID không có trong DB | `id = 999999` không tồn tại | Ném `AppException` với `ErrorCode.VOUCHER_NOT_FOUND`. |
| 17 | **ORD_17** | VoucherService (Xem voucher) | `getVoucherByCode` | Lấy chi tiết voucher thành công theo code | Kiểm tra lấy thông tin voucher theo code | `code = "GETCODE1"` tồn tại trong DB | Trả về `VoucherResponse` với đúng `discountType` và `discountValue`. |
| 18 | **ORD_18** | VoucherService (Xem voucher) | `getVoucherByCode` | Lấy voucher thất bại do code không tồn tại | Kiểm tra lỗi khi code không có trong DB | `code = "CODE_NOT_EXIST_99999"` | Ném `AppException` với `ErrorCode.VOUCHER_NOT_FOUND`. |
| 19 | **ORD_19** | VoucherService (Xem voucher) | `getAllVouchers` | Lấy toàn bộ danh sách voucher thành công | Kiểm tra trả về đủ tất cả voucher trong DB | Đã tạo sẵn 3 voucher (cả active và inactive) | Trả về `List<VoucherResponse>` chứa ít nhất 3 voucher. |
| 20 | **ORD_20** | VoucherService (Xem voucher) | `getAllVouchers(Pageable)` | Lấy danh sách voucher phân trang thành công | Kiểm tra kết quả phân trang chính xác | Đã tạo sẵn 5 voucher trong DB<br>`PageRequest pageRequest = PageRequest.of(0, 2);` | Trả về `Page<VoucherResponse>` với `size=2`, `totalElements>=5`. |
| 21 | **ORD_21** | VoucherService (Xem voucher) | `getActiveVouchers` | Chỉ trả về các voucher đang active | Kiểm tra lọc voucher có `status=1` và trong thời hạn | 2 voucher active (`status=1`, trong thời hạn) + 1 voucher inactive (`status=0`) | Kết quả chỉ chứa voucher `status=1`, không có voucher `INACTIVE1`. |
| 22 | **ORD_22** | VoucherService (Xoá voucher) | `deleteVoucher` | Xóa voucher thành công theo ID hợp lệ | Kiểm tra xóa voucher khỏi DB | Voucher `id` tồn tại trong DB | Xóa thành công, `voucherRepository.existsById(id) = false`. |
| 23 | **ORD_23** | VoucherService (Xoá voucher) | `deleteVoucher` | Xóa thất bại do ID voucher không tồn tại | Kiểm tra lỗi khi xóa voucher ID không có trong DB | `id = 999999` không tồn tại | Ném `AppException` với `ErrorCode.VOUCHER_NOT_FOUND`. |
| 24 | **ORD_24** | UserVoucherService (Xem toàn bộ voucher) | `getUserVouchers` | Lấy toàn bộ voucher của user thành công | Kiểm tra trả về cả voucher đã dùng và chưa dùng | User `"user-ga01"` có 1 voucher `isUsed=false` và 1 voucher `isUsed=true` trong DB | Trả về danh sách 2 phần tử gồm cả 2 loại. |
| 25 | **ORD_25** | UserVoucherService (Xem toàn bộ voucher) | `getUserVouchers` | Trả về danh sách rỗng khi user không có voucher | Kiểm tra list rỗng khi ví trống | User `"user-ga02-no-voucher"` chưa có voucher nào | Trả về `List` rỗng (không ném exception). |
| 26 | **ORD_26** | UserVoucherService (Xem voucher chưa dùng) | `getActiveUserVouchers` | Chỉ trả về voucher chưa dùng | Kiểm tra lọc voucher có `isUsed=false` | User `"user-st01"` có 2 voucher `isUsed=false` + 1 voucher `isUsed=true` | Trả về danh sách 2 phần tử, tất cả có `isUsed=false`, không chứa voucher đã dùng. |
| 27 | **ORD_27** | UserVoucherService (Xem voucher chưa dùng) | `getActiveUserVouchers` | Trả về rỗng khi tất cả voucher của user đã được dùng | Kiểm tra list rỗng khi không còn voucher chưa dùng | User `"user-st02"` có 2 voucher đều `isUsed=true` | Trả về `List` rỗng (không ném exception). |
| 28 | **ORD_28** | UserVoucherService (Đánh dấu đã dùng) | `markVoucherUsed` | Đánh dấu voucher đã dùng thành công | Kiểm tra `isUsed=true` và `usedAt` được ghi nhận sau khi dùng | User `"user-mk01"` có voucher `isUsed=false` trong ví<br>`userVoucherService.markVoucherUsed("user-mk01", voucher.getId());` | `isUsed=true`, `usedAt` không null, voucher không còn trong danh sách chưa dùng. |
| 29 | **ORD_29** | UserVoucherService (Đánh dấu đã dùng) | `markVoucherUsed` | Thất bại do voucher không có trong ví user | Kiểm tra lỗi khi dùng voucher chưa được claim | User `"user-mk02"` chưa claim voucher<br>`userVoucherService.markVoucherUsed("user-mk02", voucher.getId());` | Ném `AppException` với `ErrorCode.VOUCHER_NOT_IN_WALLET`. |
| 30 | **ORD_30** | UserVoucherService (Claim voucher) | `claimVoucher` | Claim voucher thành công, isUsed=false | Kiểm tra claim hợp lệ và trạng thái ban đầu là chưa dùng | Voucher active, rank đủ, chưa vượt limit<br>`userVoucherService.claimVoucher("user-cl01", voucher.getId(), MembershipRank.BRONZE);` | Voucher xuất hiện trong ví, `isUsed=false`, có trong danh sách chưa dùng. |
| 31 | **ORD_31** | UserVoucherService (Claim voucher) | `claimVoucher` | Thất bại do voucherId không tồn tại | Kiểm tra lỗi khi ID voucher không có trong DB | `voucherId = 999999` không tồn tại<br>`userVoucherService.claimVoucher("user-cl02", 999999L, MembershipRank.BRONZE);` | Ném `AppException` với `ErrorCode.VOUCHER_NOT_FOUND`. |
| 32 | **ORD_32** | UserVoucherService (Claim voucher) | `claimVoucher` | Thất bại do voucher không active (status=0) | Kiểm tra lỗi khi voucher bị vô hiệu hóa | Voucher `status=0`<br>`userVoucherService.claimVoucher("user-cl03", voucher.getId(), MembershipRank.BRONZE);` | Ném `AppException` với `ErrorCode.VOUCHER_NOT_ACTIVE`. |
| 33 | **ORD_33** | UserVoucherService (Claim voucher) | `claimVoucher` | Thất bại do rank user thấp hơn minRankRequired | Kiểm tra phân cấp thành viên khi claim | Voucher yêu cầu `minRankRequired=GOLD`, user có `MembershipRank.BRONZE`<br>`userVoucherService.claimVoucher("user-cl04", voucher.getId(), MembershipRank.BRONZE);` | Ném `AppException` với `ErrorCode.VOUCHER_RANK_NOT_ENOUGH`. |
| 34 | **ORD_34** | UserVoucherService (Claim voucher) | `claimVoucher` | Thất bại do vượt giới hạn claim mỗi user | Kiểm tra `maxPerUser` | Voucher `maxPerUser=1`, user đã claim 1 lần trước<br>`userVoucherService.claimVoucher("user-cl05", voucher.getId(), MembershipRank.BRONZE);` | Ném `AppException` với `ErrorCode.VOUCHER_USER_USAGE_LIMIT_REACHED`. |
| 35 | **ORD_35** | UserVoucherService (Claim voucher) | `claimVoucher` | Thất bại do vượt giới hạn tổng maxUsage | Kiểm tra `maxUsage` | Voucher `maxUsage=1`, đã có 1 bản ghi claim trong DB<br>`userVoucherService.claimVoucher("user-cl06", voucher.getId(), MembershipRank.BRONZE);` | Ném `AppException` với `ErrorCode.VOUCHER_USAGE_LIMIT_REACHED`. |
| 36 | **ORD_36** | CartService (Lấy giỏ hàng) | `getOrCreateCart` | Lấy giỏ hàng thành công khi user đã có giỏ | Kiểm tra trả về đúng giỏ hàng cũ | User `"user-cart-01"` đã có `CartEntity` trong DB | Trả về `CartResponse` với đúng `id` và `userId`. |
| 37 | **ORD_37** | CartService (Lấy giỏ hàng) | `getOrCreateCart` | Tự động tạo giỏ hàng mới khi user chưa có | Kiểm tra auto-create giỏ hàng rỗng | User `"user-cart-02-new"` chưa có giỏ hàng<br>`cartService.getOrCreateCart("user-cart-02-new");` | Tạo giỏ mới, `items` rỗng, lưu vào DB, trả về `CartResponse`. |
| 38 | **ORD_38** | CartService (Lấy giỏ hàng) | `getOrCreateCart` | Gọi hàm với userId không tồn tại trong hệ thống | Kiểm tra service không validate userId qua IdentityService | `userId = "invalid-user-999999"` (không tồn tại trong IdentityService) | Service vẫn tạo giỏ hàng (FAILED nếu hệ thống phải validate userId qua IdentityService). |
| 39 | **ORD_39** | CartService (Thêm vào giỏ) | `addToCart` | Thêm sản phẩm mới vào giỏ hàng thành công | Kiểm tra thêm item chưa có trong giỏ | `AddToCartRequest request = AddToCartRequest.builder().productId(100L).variantId(200L).quantity(2).price(150000L).attributesName("Đen, XL").build();` | Giỏ có 1 item, `quantity=2`, `totalAmount=300000`. |
| 40 | **ORD_40** | CartService (Thêm vào giỏ) | `addToCart` | Sản phẩm đã có trong giỏ → cộng dồn số lượng và cập nhật giá | Kiểm tra logic merge item trùng `productId+variantId` | Lần 1: `productId=100, variantId=200, qty=1, price=100000`<br>Lần 2: `productId=100, variantId=200, qty=3, price=120000` | Vẫn 1 dòng item, `quantity=1+3=4`, `priceSnapshot=120000` (giá mới), `totalAmount=480000`. |
| 41 | **ORD_41** | CartService (Thêm vào giỏ) | `addToCart` | Thất bại do sản phẩm không tồn tại | Kiểm tra service không gọi ProductServiceClient để validate | `AddToCartRequest request = AddToCartRequest.builder().productId(999999L).quantity(1).price(100000L).build();` | Ném Exception (FAILED nếu hệ thống không validate productId qua ProductServiceClient). |
| 42 | **ORD_42** | CartService (Sửa giỏ hàng) | `updateCartItem` | Cập nhật số lượng sản phẩm thành công | Kiểm tra update `quantity` hợp lệ | `itemId` hợp lệ, thuộc user<br>`UpdateCartItemRequest request = UpdateCartItemRequest.builder().quantity(5).build();` | `quantity=5`, `totalAmount` tính lại đúng. |
| 43 | **ORD_43** | CartService (Sửa giỏ hàng) | `updateCartItem` | Thất bại do itemId không tồn tại | Kiểm tra lỗi khi ID item sai | `itemId = 999999` không tồn tại<br>`cartService.updateCartItem("user-upd-02", 999999L, request);` | Ném `AppException` với `ErrorCode.CART_ITEM_NOT_FOUND`. |
| 44 | **ORD_44** | CartService (Sửa giỏ hàng) | `updateCartItem` | Thất bại do item thuộc giỏ hàng của user khác | Kiểm tra phân quyền sở hữu item trong giỏ | Item của `"user-upd-03A"`, user `"user-upd-03B"` cố sửa<br>`cartService.updateCartItem("user-upd-03B", itemId, request);` | Ném `AppException` với `ErrorCode.CART_ITEM_NOT_BELONG_TO_USER`. |
| 45 | **ORD_45** | CartService (Sửa giỏ hàng) | `updateCartItem` | Thất bại do số lượng bằng 0 hoặc âm | Kiểm tra validate số lượng không hợp lệ | `UpdateCartItemRequest request = UpdateCartItemRequest.builder().quantity(0).build();` | Ném `AppException` với `ErrorCode.CART_ITEM_QUANTITY_INVALID`. |
| 46 | **ORD_46** | CartService (Xoá giỏ hàng) | `removeFromCart` | Xóa 1 sản phẩm khỏi giỏ hàng thành công | Kiểm tra xóa item theo ID hợp lệ | `itemId` hợp lệ, thuộc `"user-del-01"`<br>`cartService.removeFromCart("user-del-01", itemId);` | Giỏ hàng rỗng sau khi xóa, `totalAmount=0`. |
| 47 | **ORD_47** | CartService (Xoá giỏ hàng) | `removeFromCart` | Thất bại do itemId không tồn tại | Kiểm tra lỗi khi ID item sai | `itemId = 999999` không tồn tại<br>`cartService.removeFromCart("user-del-02", 999999L);` | Ném `AppException` với `ErrorCode.CART_ITEM_NOT_FOUND`. |
| 48 | **ORD_48** | CartService (Xoá giỏ hàng) | `clearCart` | Xóa toàn bộ sản phẩm trong giỏ thành công | Kiểm tra clear tất cả item trong giỏ | User `"user-del-03"` có 2 sản phẩm khác nhau<br>`cartService.clearCart("user-del-03");` | Giỏ hàng rỗng sau khi clear, `items.isEmpty()=true`. |
| 49 | **ORD_49** | CartService (Xoá giỏ hàng) | `clearCart` | Thất bại do giỏ hàng không tồn tại | Kiểm tra lỗi khi user chưa có giỏ hàng | User `"user-del-04-no-cart"` chưa từng tạo giỏ hàng<br>`cartService.clearCart("user-del-04-no-cart");` | Ném `AppException` với `ErrorCode.CART_NOT_FOUND`. |
| 50 | **ORD_50** | OrderService (Tạo đơn hàng) | `createOrder` | Tạo đơn NORMAL thành công khi tồn kho đủ | Kiểm tra tạo đơn với đầy đủ thông tin hợp lệ | `when(productServiceClient.checkStockAvailability(anyLong(), anyInt())).thenReturn(true)`<br>`OrderRequest request = OrderRequest.builder().addressId(1L).paymentMethod(PaymentMethod.COD).shippingFee(30000L).items(List.of(item)).build();` | Tạo thành công, `status=PENDING`, `paymentStatus=UNPAID`, trả về `OrderResponse`. |
| 51 | **ORD_51** | OrderService (Tạo đơn hàng) | `createOrder` | Thất bại do hết hàng hoàn toàn (tồn kho = 0) | Kiểm tra chặn đơn NORMAL khi tồn kho bằng 0 | `when(productServiceClient.checkStockAvailability(anyLong(), anyInt())).thenReturn(false)`<br>Item `quantity=1` | Ném `AppException` với `ErrorCode.INSUFFICIENT_STOCK`. |
| 52 | **ORD_52** | OrderService (Tạo đơn hàng) | `createOrder` | Thất bại do số lượng đặt vượt quá tồn kho | Kiểm tra chặn khi đặt nhiều hơn kho có sẵn (kho=5, đặt=10) | `when(productServiceClient.checkStockAvailability(eq(100L), eq(10))).thenReturn(false)`<br>Item `variantId=100L, quantity=10` | Ném `AppException` với `ErrorCode.INSUFFICIENT_STOCK`. |
| 53 | **ORD_53** | OrderService (Tạo đơn hàng) | `createOrder` | Thất bại do variantId null | Kiểm tra validate biến thể sản phẩm bắt buộc | `OrderItemRequest item = OrderItemRequest.builder().productId(1L).variantId(null).quantity(1).price(100000L).build();` | Ném `AppException` với `ErrorCode.PRODUCT_VARIANT_NOT_FOUND`. |
| 54 | **ORD_54** | OrderService (Tạo đơn hàng) | `createOrder` | Kiểm tra tính toán total = subtotal - discount + shippingFee | Kiểm tra công thức tính tổng tiền đơn hàng | `when(checkStock).thenReturn(true)`<br>Item 1: `qty=2, price=100000` → subtotal +200000<br>Item 2: `qty=1, price=50000` → subtotal +50000<br>`shippingFee=30000`, không voucher | `subtotal=250000`, `discount=0`, `shippingFee=30000`, `total=280000`. |
| 55 | **ORD_55** | OrderService (Tạo đơn hàng) | `createOrder` | Đơn hàng mới tạo phải có status=PENDING và paymentStatus=UNPAID | Kiểm tra trạng thái mặc định khi tạo đơn | Đơn hàng tạo thành công với dữ liệu hợp lệ | `status=PENDING`, `paymentStatus=UNPAID`, `createdAt` không null. |
| 56 | **ORD_56** | OrderService (Tạo đơn hàng) | `createOrder` | Thất bại khi danh sách items rỗng (Bug Finding) | Kiểm tra validate danh sách sản phẩm bắt buộc | `OrderRequest request = OrderRequest.builder().paymentMethod(PaymentMethod.COD).items(List.of()).build();` | Ném Exception (FAILED nếu hệ thống tạo đơn thành công với `total=0`). |
| 57 | **ORD_57** | OrderService (Xem đơn hàng) | `getOrderById` | Lấy đơn hàng thành công theo ID hợp lệ | Kiểm tra lấy đúng thông tin đơn hàng | `OrderEntity` tồn tại trong DB<br>`orderService.getOrderById(saved.getId());` | Trả về `OrderResponse` với đầy đủ thông tin và danh sách items. |
| 58 | **ORD_58** | OrderService (Xem đơn hàng) | `getOrderById` | Lấy đơn thất bại do ID không tồn tại | Kiểm tra lỗi khi ID không có trong DB | `id = 999999` không tồn tại<br>`orderService.getOrderById(999999L);` | Ném `AppException` với `ErrorCode.ORDER_NOT_FOUND`. |
| 59 | **ORD_59** | OrderService (Xem đơn hàng) | `getAllOrders` | Lấy toàn bộ danh sách đơn hàng thành công | Kiểm tra trả về đủ tất cả đơn hàng trong DB | Đã tạo sẵn 3 đơn hàng với userId khác nhau | Trả về `List<OrderResponse>` chứa ít nhất 3 đơn. |
| 60 | **ORD_60** | OrderService (Xem đơn hàng) | `getAllOrders(Pageable)` | Lấy danh sách đơn hàng phân trang thành công | Kiểm tra kết quả phân trang chính xác | Đã tạo sẵn 5 đơn hàng trong DB<br>`PageRequest pageRequest = PageRequest.of(0, 2);` | Trả về `Page<OrderResponse>` với `size=2`, `totalElements>=5`. |
| 61 | **ORD_61** | OrderService (Xem đơn hàng) | `getOrdersByUserId` | Lấy danh sách đơn theo userId thành công | Kiểm tra lọc đơn hàng theo user | User `"user-get-04A"` có 2 đơn, user `"user-get-04B"` có 1 đơn<br>`orderService.getOrdersByUserId("user-get-04A");` | Trả về 2 đơn của user A, tất cả có `userId="user-get-04A"`. |
| 62 | **ORD_62** | OrderService (Xem đơn hàng) | `getOrdersByStatus` | Lấy danh sách đơn theo trạng thái thành công | Kiểm tra lọc đơn theo `status=PENDING` | 2 đơn `status=PENDING` + 1 đơn `status=PAID` trong DB<br>`orderService.getOrdersByStatus(OrderStatus.PENDING);` | Chỉ trả về các đơn `status=PENDING`, không có đơn `PAID`. |
| 63 | **ORD_63** | OrderService (Đổi trạng thái) | `updateOrderStatus` | Chuyển trạng thái PENDING → PAID thành công | Kiểm tra lộ trình hợp lệ và ghi nhận `paidAt` | Đơn `status=PENDING`<br>`orderService.updateOrderStatus(order.getId(), OrderStatus.PAID);` | `status=PAID`, `paymentStatus=PAID`, `paidAt` không null. |
| 64 | **ORD_64** | OrderService (Đổi trạng thái) | `updateOrderStatus` | Chuyển trạng thái PAID → SHIPPING thành công | Kiểm tra lộ trình hợp lệ và ghi nhận `shippedAt` | Đơn `status=PAID`<br>`orderService.updateOrderStatus(order.getId(), OrderStatus.SHIPPING);` | `status=SHIPPING`, `shippedAt` không null. |
| 65 | **ORD_65** | OrderService (Đổi trạng thái) | `updateOrderStatus` | Chuyển trạng thái SHIPPING → COMPLETED thành công | Kiểm tra lộ trình hợp lệ và ghi nhận `completedAt` | Đơn `status=SHIPPING`<br>`orderService.updateOrderStatus(order.getId(), OrderStatus.COMPLETED);` | `status=COMPLETED`, `completedAt` không null. |
| 66 | **ORD_66** | OrderService (Đổi trạng thái) | `updateOrderStatus` | Hủy đơn hàng từ trạng thái PENDING thành công | Kiểm tra hủy đơn và ghi nhận `cancelledAt` | Đơn `status=PENDING`<br>`orderService.updateOrderStatus(order.getId(), OrderStatus.CANCELLED);` | `status=CANCELLED`, `cancelledAt` không null. |
| 67 | **ORD_67** | OrderService (Đổi trạng thái) | `updateOrderStatus` | Thất bại do chuyển trạng thái sai lộ trình | Kiểm tra chặn bỏ qua bước trung gian PENDING → COMPLETED | Đơn `status=PENDING`, cố chuyển thẳng sang `COMPLETED`<br>`orderService.updateOrderStatus(order.getId(), OrderStatus.COMPLETED);` | Ném `AppException` với `ErrorCode.ORDER_INVALID_STATUS_TRANSITION`. |
| 68 | **ORD_68** | OrderService (Đổi trạng thái) | `updateOrderStatus` | Thất bại do hủy đơn hàng đã COMPLETED | Kiểm tra chặn hủy đơn đã hoàn tất | Đơn `status=COMPLETED`<br>`orderService.updateOrderStatus(order.getId(), OrderStatus.CANCELLED);` | Ném `AppException` với `ErrorCode.ORDER_CANNOT_CANCEL_COMPLETED`. |
| 69 | **ORD_69** | OrderService (Đổi trạng thái) | `updateOrderStatus` | Thất bại do hủy đơn hàng đã bị CANCELLED | Kiểm tra chặn hủy đơn lần 2 | Đơn `status=CANCELLED`<br>`orderService.updateOrderStatus(order.getId(), OrderStatus.CANCELLED);` | Ném `AppException` với `ErrorCode.ORDER_ALREADY_CANCELLED`. |
| 70 | **ORD_70** | OrderService (Đổi trạng thái) | `updateOrderStatus` | Thất bại do ID đơn hàng không tồn tại | Kiểm tra lỗi khi ID đơn không có trong DB | `id = 999999` không tồn tại<br>`orderService.updateOrderStatus(999999L, OrderStatus.PAID);` | Ném `AppException` với `ErrorCode.ORDER_NOT_FOUND`. |

